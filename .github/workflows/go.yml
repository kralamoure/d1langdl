name: Go

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build_test:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Go environment
        uses: actions/setup-go@v1
        with:
          go-version: '1.16.*'

      - name: Build
        run: go build -v ./cmd/...

      - name: Test
        run: go test -v ./...
